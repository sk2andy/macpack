{
  "name": "macpack",
  "version": "0.3.0",
  "description": "Universal macOS package manager CLI for Homebrew, npm, bun, uv, and git repository manifests.",
  "type": "module",
  "bin": {
    "macpack": "./dist/index.js"
  },
  "files": [
    "dist",
    "docs",
    "examples",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "node scripts/sync-version.mjs && tsc -p tsconfig.build.json && chmod +x dist/index.js",
    "build:executable": "node scripts/build-macos-arm64-executable.mjs",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "test": "vitest run src",
    "prepack": "npm run build"
  },
  "keywords": [
    "macos",
    "homebrew",
    "brew",
    "npm",
    "bun",
    "uv",
    "git",
    "repositories",
    "cli",
    "package-manager"
  ],
  "author": "sk2andy",
  "license": "MIT",
  "dependencies": {
    "@clack/prompts": "^1.5.1",
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "esbuild": "^0.28.1",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=20"
  },
  "volta": {
    "node": "22.22.1"
  }
}
